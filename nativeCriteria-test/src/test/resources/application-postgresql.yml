spring:
    datasource:
        dataSourceClassName: org.postgresql.ds.PGSimpleDataSource
        url: jdbc:postgresql://localhost:5432/nativeCriteria?user=postgres&password=postgres
        databaseName: nativeCriteria
        serverName: localhost
        username: postgres
        password: postgres
    jpa:
        database: POSTGRESQL
